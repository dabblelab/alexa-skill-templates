/*
 rename this file to config.js and set the values below.
*/

const config = {};

config.region = "us-east-1",
config.accessKeyId = "your-access-key",
config.secretAccessKey = "your-secret"
config.bucketName = "your-bucket"

module.exports = config;